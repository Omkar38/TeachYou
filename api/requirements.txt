# Slim requirements for the Vercel Python serverless API function.
# Heavy pipeline deps (playwright, TTS libs, etc.) are only needed by the
# Celery worker, which runs outside Vercel (e.g. on Railway).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
alembic>=1.13
python-multipart>=0.0.9
celery>=5.3
redis>=5.0
pypdf>=4.0
pillow>=10.0
httpx>=0.27
psycopg2-binary>=2.9.9
python-dotenv>=1.0
rank-bm25>=0.2.2
